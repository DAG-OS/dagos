environment:
  name: basic
  description: An environment for BDD tests of the DAG-OS CLI.
  platform:
    os:
      - linux
    images:
      - id: ubuntu
  components:
    - name: vale
      purpose: Lint some documents.
